# VirusScanner

Утилита для сканирования каталогов и выявления вредоносных файлов по MD5-хешам. Проект написан на **C++17** с использованием **CMake**. Основная логика вынесена в **динамическую библиотеку (scanner)**, а интерфейс командной строки (CLI) реализован отдельным модулем. Также предусмотрены тесты на **GoogleTest**.

## 🚀 Быстрый старт

```bash
# Клонирование репозитория
git clone https://github.com/bk-ru/virus_scanner.git
cd virus_scanner

# Сборка (требуется OpenSSL)
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build

# Запуск
./build/scanner --base base.csv --log scan.log --path /путь/к/сканированию
```

## 📋 Что делает утилита

1. **Сбор файлов**: рекурсивно обходит указанный каталог
2. **Вычисление хеша**: для каждого файла вычисляет MD5-хеш
3. **Проверка**: сравнивает хеш с базой известных вредоносных сигнатур (CSV)
4. **Логирование**: записывает обнаруженные угрозы в текстовый лог
5. **Отчет**: выводит сводную статистику по завершении

## 📁 Структура проекта

```
virus_scanner/
├── scanner/                   # Основная библиотека (DLL)
│   ├── scanner.cpp            # Координатор процесса сканирования
│   ├── hashDatabase.cpp       # База сигнатур (хешей)
│   ├── logger.cpp             # Подсистема логирования
│   ├── md5Calc.cpp            # Вычисление MD5
│   ├── threadPool.cpp         # Пул потоков
│   ├── settingsValidator.cpp  # Валидация параметров
│   └── scannerConstants.h     # Константы конфигурации
├── scannerCli/                # Утилита командной строки (CLI)
│   ├── main.cpp               # Точка входа
│   ├── config.cpp             # Управление конфигурацией
│   └── lineParser.cpp         # Разбор аргументов командной строки
├── tests/                     # Тесты
│   ├── tests.cpp              # Интеграционные тесты (4)
│   └── unitTests.cpp          # Модульные тесты (27)
├── docs/                      # Документация
│   ├── ARCHITECTURE_RU.md     # Архитектура системы
│   └── images/                # Скриншоты
└── base/                      # Примеры баз хешей
    └── base.csv               # Пример базы хешей
```

## 📝 Формат базы данных хешей

База представляет собой CSV-файл, где каждая строка содержит MD5-хеш и вердикт, разделенные точкой с запятой (`;`).

**Формат**: `md5_hash;verdict`

**Пример** (`base.csv`):

```csv
a9963513d093ffb2bc7ceb9807771ad4;Exploit
ac6204ffeb36d2320e52f1d551cfa370;Dropper
8ee70903f43b227eeb971262268af5a8;Downloader
65a8e27d8879283831b664bd8b7f0ad4;Trojan
d41d8cd98f00b204e9800998ecf8427e;Worm
```

**Требования**:

* MD5-хеш должен содержать 32 символа (шестнадцатеричный формат)
* вердикт не должен быть пустым
* регистр символов в хеше не важен (автоматически приводится к нижнему)

## 💻 Использование CLI

### Справка

![Справка по использованию](docs/images/help.png)

### Аргументы командной строки

```bash
scanner [ОПЦИИ]

Опции:
  -b, --base <путь>     Путь к базе хешей (.csv) [ОБЯЗАТЕЛЬНО]
  -p, --path <путь>     Каталог для сканирования [ОБЯЗАТЕЛЬНО]
      --log <путь>      Путь к файлу лога (по умолчанию: scan.log)
  -h, --help            Показать справку
```

### Примеры использования

```bash
# Базовый запуск
scanner --base malware.csv --path /home/user/downloads --log scan.log

# Сканирование системного каталога
scanner -b signatures.csv -p C:\Windows\Temp --log temp_scan.log

# Сканирование с именем лога по умолчанию
scanner --base base.csv --path ./suspicious_files
```

## 📊 Вывод результатов

### Вывод в консоль

![Пример вывода в консоль](docs/images/print.png)

Сканер выводит:

* ✅ путь сканирования
* ✅ используемую базу хешей
* ✅ путь к файлу лога
* ✅ количество потоков
* ✅ статистику: обработано файлов, найдено угроз, ошибок
* ✅ время выполнения

### Файл лога

![Пример файла лога](docs/images/log.png)

Лог содержит:

* 📅 временную метку начала сканирования
* 🔍 подробности по каждой обнаруженной угрозе:

  * полный путь к файлу
  * MD5-хеш
  * вердикт (класс/тип угрозы)
* ⚠️ все ошибки, возникшие в процессе сканирования

## 🔧 Требования и зависимости

### Системные требования

* **ОС**: Windows 10+, Linux (Ubuntu 20.04+), macOS 10.15+
* **Компилятор**:

  * Windows: Visual Studio 2019+ (MSVC 19.20+)
  * Linux: GCC 7+ или Clang 6+
  * macOS: Xcode 11+ (Clang)
* **CMake**: 3.16 или новее
* **ОЗУ**: минимум 512 МБ

### Библиотеки

* **OpenSSL** (libcrypto) - вычисление MD5
* **GoogleTest** - фреймворк тестирования (загружается автоматически)
* **C++17 STL** - стандартная библиотека

## 📦 Установка зависимостей

### Вариант 1: vcpkg (рекомендуется)

#### Установка vcpkg

```bash
# Клонирование vcpkg
git clone https://github.com/microsoft/vcpkg.git
cd vcpkg

# Windows
bootstrap-vcpkg.bat
vcpkg integrate install

# Linux/macOS
./bootstrap-vcpkg.sh
./vcpkg integrate install
```

#### Установка пакетов

```bash
# OpenSSL
./vcpkg install openssl

# Windows (x64)
./vcpkg install openssl:x64-windows

# Linux
./vcpkg install openssl:x64-linux

# macOS
./vcpkg install openssl:x64-osx
```

### Вариант 2: Системный менеджер пакетов

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install libssl-dev cmake build-essential

# Fedora/RHEL
sudo dnf install openssl-devel cmake gcc-c++

# macOS (Homebrew)
brew install openssl cmake

# Arch Linux
sudo pacman -S openssl cmake base-devel
```

## 🏗️ Сборка проекта

### Linux / macOS

```bash
# Клонирование
git clone https://github.com/bk-ru/virus_scanner.git
cd virus_scanner

# Конфигурация (с vcpkg)
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_TOOLCHAIN_FILE=/path/to/vcpkg/scripts/buildsystems/vcpkg.cmake

# Или без vcpkg (если OpenSSL установлен в системе)
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

# Сборка
cmake --build build --config Release -j$(nproc)

# Артефакты сборки:
# build/scanner (CLI)
# build/libscanner.so (библиотека)
```

### Windows (Visual Studio)

```powershell
# Клонирование
git clone https://github.com/bk-ru/virus_scanner.git
cd virus_scanner

# Конфигурация
cmake -S . -B build `
  -G "Visual Studio 17 2022" `
  -A x64 `
  -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake

# Сборка
cmake --build build --config Release

# Артефакты сборки:
# build/bin/Release/scanner.exe (CLI)
# build/bin/Release/scanner.dll (библиотека)
```

## 🧪 Тестирование

Проект включает набор тестов:

* **4 интеграционных теста** - сквозные (E2E) проверки полного сценария
* **27 модульных тестов** - проверка отдельных компонентов

### Запуск всех тестов

```bash
cd build
ctest -C Release -V
```

### Запуск выбранных тестов

```bash
# Только модульные тесты
ctest -C Release -R UnitTests -V

# Только интеграционные тесты
ctest -C Release -R IntegrationTest -V

# Конкретный тест
ctest -C Release -R SettingsValidatorTest -V
```

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.